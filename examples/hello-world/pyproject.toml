[project]
name = "hello-world"
version = "0.0.0"
description = "Stario minimal counter — smallest Datastar example (main:bootstrap)"
requires-python = ">=3.14"
dependencies = [
    "stario>=4,<5",
]

