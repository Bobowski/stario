[project]
name = "hello-world"
version = "0.0.0"
description = "Stario hello world — one route, one HTML page (main:bootstrap)"
requires-python = ">=3.12"
dependencies = [
    "stario>=5,<6",
]

[tool.uv.sources]
stario = { path = "../../" }
