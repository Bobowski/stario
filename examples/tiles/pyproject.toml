[project]
name = "tiles"
version = "0.0.0"
description = "Stario collaborative painting board — recommended starting example (main:bootstrap)"
requires-python = ">=3.14"
dependencies = [
    "stario",
]

[tool.uv.sources]
stario = { path = "../../", editable = true }
