# Stario server runtime (see docs). Copy to .env and edit if your tooling loads it.
STARIO_HOST=127.0.0.1
STARIO_PORT=8000
STARIO_LOOP=asyncio
STARIO_TRACER=auto

# Application config goes below, for example:
# DATABASE_URL=sqlite:///app.sqlite3
