# Server benchmark baseline (2026-08-27)

Reference for comparing **Stario Cython** (llhttp) against **Stario Python**
(httptools) on the official `benchmarks/server` suite. Relative comparisons on
one machine — not lab-grade absolutes.

**Going forward:** on a given machine, compare new Cython protocol work to a
fresh unmodified `cython-core` wrk run on that machine. The 2026-08-28
same-host hot-path capture is [`baseline-20260828.md`](baseline-20260828.md).
The Cython row below remains the Python vs Cython snapshot for this date.

Native/ASGI competitor tables from the previous capture remain in
[`baseline-20260824.md`](baseline-20260824.md) (same machine class; not re-run
on this date).

## Reproduce

```bash
WRK=wrk BROTLI_PKG_CONFIG=/path/to/brotli/pkgconfig \
  RUNS=5 WARMUP=1 DURATION=10s THREADS=2 \
  CONNECTIONS=128 UPLOAD_CONNECTIONS=32 \
  ENDPOINT_TIER=all \
  benchmarks/server/run.sh stario stario-cython
```

## Environment

| | |
|---|---|
| CPU | Intel Xeon (KVM), 4 vCPUs, x86_64 |
| RAM | 16 GiB, no swap |
| OS | Linux 6.12.94+ |
| Python | 3.14.7 |
| Client | wrk 4.1.0, `-t2 -c128` (read/small upload), `-c32` (64KB/2MB) |
| Topology | wrk and servers on same host, `127.0.0.1` loopback |
| Commit | `485394d` (`cursor/cython-protocol-locality-d037`) |
| Run | `20260827T181833Z` (read / small POST); `20260827T204216Z` (64KB / 2MB / multipart) |

## Methodology

- One worker/process per target.
- 10 endpoints: read (`plaintext`, `json`, `params`) + upload (`validate`,
  `post-form`, `post-json-1k`, `post-octet-64k`, `post-octet-2m`,
  `post-stream-2m`, `multipart-2m`).
- **5 measured + 1 warmup**, `DURATION=10s`, IQR trimming via
  `benchmarks/server/stats.py`.
- Handlers **compute responses per request** (no reused/static `Response`
  objects, no pre-serialized JSON bytes). See `benchmarks/README.md` handler
  policy.
- `wrk` reports `Requests/sec` only. Status and body were probed separately
  on this branch for the official 10 routes.

## Stario Cython vs Stario Python

| Endpoint | Python httptools | Cython llhttp | Cython / Python |
| --- | ---: | ---: | ---: |
| Plaintext | 72,734 ± 647 | 125,160 ± 298 | **1.72×** |
| JSON | 71,042 ± 582 | 123,017 ± 3,211 | **1.73×** |
| Params | 71,033 ± 1,669 | 117,136 ± 2,590 | **1.65×** |
| Validate JSON | 56,663 ± 1,621 | 69,992 ± 838 | **1.24×** |
| Form POST | 60,476 ± 1,105 | 73,163 ± 361 | **1.21×** |
| JSON 1KB | 56,451 ± 276 | 66,933 ± 104 | **1.19×** |
| Octet 64KB | 25,228 ± 219 | 24,229 ± 244 | **0.96×** |
| Octet 2MB (buffer) | 2,062 ± 292 | 2,947 ± 35 | **1.43×** |
| Octet 2MB (stream) | 2,863 ± 15 | 3,208 ± 8 | **1.12×** |
| Multipart 2MB | 2,047 ± 173 | 2,905 ± 60 | **1.42×** |

Read paths are ~**1.7×**. Small POST (validate / form / JSON 1KB) is ~**1.2×**.
64KB ingest is even. Buffered 2MB, streaming 2MB, and multipart are **ahead of Python**
after pre-sizing Content-Length bodies and pausing `body()` at 64KiB so `_pump_data`
yields between 512KiB parser quantums (`485394d`, run `20260827T204216Z`).

## Stario group (from `run.sh` summary)

### Read-heavy

| Server | Plaintext | JSON | Params |
| --- | ---: | ---: | ---: |
| Stario (Python httptools) | 72,734 ± 647 | 71,042 ± 582 | 71,033 ± 1,669 |
| Stario (Cython llhttp) | 125,160 ± 298 | 123,017 ± 3,211 | 117,136 ± 2,590 |

### Upload / body

| Server | Validate JSON | Form POST | JSON 1KB | Octet 64KB | Octet 2MB (buffer) | Octet 2MB (stream) | Multipart 2MB |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| Stario (Python httptools) | 56,663 ± 1,621 | 60,476 ± 1,105 | 56,451 ± 276 | 25,228 ± 219 | 2,062 ± 292 | 2,863 ± 15 | 2,047 ± 173 |
| Stario (Cython llhttp) | 69,992 ± 838 | 73,163 ± 361 | 66,933 ± 104 | 24,229 ± 244 | 2,947 ± 35 | 3,208 ± 8 | 2,905 ± 60 |

## Notes

- Cython row is the comparison anchor for later protocol work on this machine.
- Upload columns for 64KB / 2MB / multipart were re-measured after `485394d`
  (Content-Length pre-size + `body()` 64KiB pause). Read and small-POST columns
  are the original `20260827T181833Z` suite.
- Competitor (Socketify, Granian, Sanic, …) numbers: see
  [`baseline-20260824.md`](baseline-20260824.md).
