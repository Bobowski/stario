# Server benchmarks — 2026-08-29 (sync handlers)

Cloud agent host, not the 2026-08-28 lab machine. Same runner knobs
except `RUNS=5` / `WARMUP=1` (lab used 7 / 2). One worker, `10s`,
`THREADS=2`, `CONNECTIONS=128`. IQR trimming. Git `0358b5a` on
`cursor/sync-handlers-7ae2`.

GET handlers in `apps/stario_app.py` are **sync** (`def`, not
`async def`). POST handlers stay async (`await c.req.body()`).

Full runner log: `results/20260829T085500Z/`.

## Read-heavy (this host)

Median req/s. Bold is best in that column.

| Server | Plaintext | JSON | Params |
| --- | ---: | ---: | ---: |
| Granian RSGI | **146,463** | **147,058** | **150,545** |
| Stario Cython (sync GET) | 134,544 | 129,148 | 125,733 |
| Socketify | 120,851 | 101,599 | 80,839 |
| BlackSheep+Granian | 115,574 | 110,135 | 107,014 |
| Stario Python (sync GET) | 80,459 | 77,047 | 75,412 |
| Sanic | 52,230 | 46,284 | 45,676 |

Stario / Granian on this host: plaintext **0.92×**, JSON **0.88×**,
params **0.84×**.

## Same-host A/B vs `cython-core` (no sync handlers)

Sequential, this host, same knobs (`10s` × 5 + 1 warmup).
`cython-core` `de72935` (`20260829T093821Z`) → this branch `954e1de`
(`20260829T094433Z`).

| Endpoint | Python before | Python sync | Ratio | Cython before | Cython sync | Ratio |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| Plaintext | 72,991 | 76,836 | **1.05×** | 142,454 | 140,653 | 0.99× |
| JSON | 71,859 | 74,139 | **1.03×** | 136,840 | 138,375 | 1.01× |
| Params | 73,844 | 75,377 | **1.02×** | 122,843 | 124,882 | 1.02× |

Python httptools did not eager-start tasks, so inline sync dispatch shows
up. Cython already ran no-await handlers inside `create_task(...,
eager_start=True)`; skipping the Task is inside run-to-run noise
(baseline Cython plaintext was ±10k).

Do not compare these req/s to lab `baseline-20260828` — different machine.

## Small POST (async handlers, this host)

`results/20260829T091344Z/`. Same knobs. Stario POST routes still
`async def` + `await c.req.body()`.

| Server | Validate JSON | Form POST | JSON 1KB |
| --- | ---: | ---: | ---: |
| Stario Cython | **107,795** | **135,738** | **117,477** |
| Granian RSGI | 103,980 | 120,241 | 108,311 |

Async path is intact and still ahead of Granian on these three.
